//
// config/index.js
// App-level configuration — all settings one place-la!
// Backend URL change aana, idha mattum change panniduvom.
//

const config = {
  // Your Express backend URL. Change when deployed.
  API_BASE_URL: "https://trimtracker01.onrender.com/api",

  APP_NAME: "TrimTracker",
  VERSION: "1.0.0",
};

export default config;
